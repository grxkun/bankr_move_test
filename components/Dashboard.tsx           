import React from 'react'
import { useSuiContext } from '../context/SuiContext'

export default function Dashboard() {
  const { wallet } = useSuiContext()
  // TODO: fetch balances and actions via Sui contracts

  return (
    <section className="bg-white rounded shadow p-4">
      <h2 className="text-xl font-bold mb-2">Dashboard</h2>
      {wallet ? (
        <div>
          <div className="mb-2">Wallet: <span className="font-mono">{wallet.address}</span></div>
          <div>Balances: {/* TODO: Show balances */} <span className="italic">Coming soon</span></div>
          <div>Actions: {/* TODO: Actions UI */} <span className="italic">Coming soon</span></div>
        </div>
      ) : (
        <p className="text-gray-500">Connect your wallet to view dashboard.</p>
      )}
    </section>
  )
}